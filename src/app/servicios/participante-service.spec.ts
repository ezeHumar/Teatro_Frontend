import { ParticipanteService } from './participante-service';

describe('ParticipanteService', () => {
  it('should create an instance', () => {
    expect(new ParticipanteService()).toBeTruthy();
  });
});
